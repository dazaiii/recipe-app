import { ConvertMinutesToHoursPipe } from './convert-minutes-to-hours.pipe';

describe('ConvertMinutesToHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMinutesToHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
